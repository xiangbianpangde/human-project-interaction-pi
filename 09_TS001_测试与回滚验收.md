---
type: test-contract
contract_id: TS1-TEST-001
revision: 1
status: approved
created: 2026-08-29
updated: 2026-08-29
task_slices: [TS001-IMPL, TS001-VAL]
test_status: NOT-RUN
---

# TS-001 测试与回滚验收合同

## 1. 目标与范围

本文件把 `TS001-IMPL` / `TS001-VAL` 的最小合同变成可逐条执行、可独立复核的测试清单，覆盖四组：

1. Schema；
2. 权限与引用；
3. Handoff / Result 幂等；
4. 回滚与恢复程序。

本切片只测试合同、纯数据 fixture、提交流程和回滚验收，不实现或测试 filesystem gate、Run、Evidence、Claim、Research Event 的完整运行时闭环。

所有用例当前为 `NOT-RUN`。负向用例的正确结果是拒绝、保留测试证据且不产生 accepted canonical mutation；测试不得触碰真实 canonical 路径。

## 2. 用例编号与记录要求

- `TS1-S-*`：Schema；
- `TS1-P-*`：Permission / Reference；
- `TS1-I-*`：Idempotency / Handoff / Result；
- `TS1-R-*`：Rollback / Recovery procedure。

每条用例必须记录：输入 fixture、输入 SHA、实际命令、运行时及版本、输出/日志 SHA、退出码和起止时间。工具退出 0 不能单独证明合同通过，必须同时核对语义结果、拒绝/留痕和越界写入情况。

## 3. 不变量覆盖

### 3.1 TS-001 直接覆盖

| 不变量 | 测试指针 | 归属 | 预期 |
|---|---|---|---|
| INV-002 | `TS1-S-010` | TS-001 | 重复 entity_id 拒绝 |
| INV-004 | `TS1-P-001` | TS-001 | spec_ref 不可解析时拒绝 |
| INV-005 | `TS1-P-002` | TS-001 | artifact 不存在或 hash 不符时拒绝 |
| INV-007 | `TS1-P-003` | TS-001 | allowlist 外路径拒绝 |
| INV-011 | `TS1-I-003`、`TS1-I-004` | TS-001 | 重发幂等，retry 新 attempt |
| INV-012 | `TS1-S-011` | TS-001 | integrity rule/schema/Gate 缺失时 fail closed |
| INV-016 | `TS1-S-008`、`TS1-P-006` | TS-001 | 未知或未登记 data_class 拒绝 |

### 3.2 后置不变量

以下不变量不在 TS-001 实测，但必须登记后续归属，不得被本切片的通过状态覆盖：

| 不变量 | 后续归属 | 后置原因 |
|---|---|---|
| INV-001 | TS-002 | 需要 terminal RunManifest 与内容 hash |
| INV-003 | TS-002 | 需要 Harness canonical mutation 与 Event 事务 |
| INV-006 | TS-003 | 需要 EvidenceCard / Claim 引用链 |
| INV-008 | TS-002 | 需要 materialized state / derived index rebuild |
| INV-009 | TS-002 / TS-003 | 需要定义 revision 与 Run binding |
| INV-010 | TS-003 | 需要上游 revision 与 stale 传播 |
| INV-013 | TS-003 | 需要 Evidence / Claim Gate |
| INV-014 | TS-003 | 需要 EvidenceSnapshot 冻结机制 |
| INV-015 | TS-003 | 需要用户人类验收状态 |
| INV-017 | TS-002 | 需要受控拒绝记录与 canonical transaction |

INV-001、003、006、009、010、013、014、015 的完整实测明确后置到 TS-002 / TS-003；INV-008 和 INV-017 也不在本切片执行。

## 4. 组 1：Schema（`TS1-S-*`）

| ID | 输入/刺激 | 预期结果 | 关联 |
|---|---|---|---|
| TS1-S-001 | 合法 TaskSlice fixture | Schema 通过，字段完整 | `TaskSlice` |
| TS1-S-002 | 合法 HandoffBundle fixture | Schema 通过，信封字段完整 | `HandoffBundle` |
| TS1-S-003 | 合法 ResultBundle fixture | Schema 通过，提交字段完整 | `ResultBundle` |
| TS1-S-004 | 合法 ExperimentSpec 只读 fixture | Schema 通过，只读引用可解析 | `ExperimentSpec` |
| TS1-S-005 | 删除任一必填字段 | 拒绝，指出字段，不产生 accepted mutation | 四份 Schema |
| TS1-S-006 | 将字段替换为错误类型 | 拒绝，指出类型错误 | 四份 Schema |
| TS1-S-007 | 使用不符合格式的 `TS001-IMPL` / `TS001-VAL` ID | 拒绝 | identity |
| TS1-S-008 | 使用封闭枚举外或缺失的 `data_class` | 拒绝 | INV-016 |
| TS1-S-009 | VAL verdict 使用词表外值或 `PASS` / `approved` | 拒绝 | verdict contract |
| TS1-S-010 | 两条记录使用相同 entity_id | 拒绝 | INV-002 |
| TS1-S-011 | 删除 required integrity rule、Schema 或 Gate 配置 | fail closed，停止相关能力 | INV-012 |

## 5. 组 2：权限与引用（`TS1-P-*`）

| ID | 输入/刺激 | 预期结果 | 关联 |
|---|---|---|---|
| TS1-P-001 | TaskSlice 的 `spec_ref` 指向不存在的对象/版本 | 拒绝 | INV-004 |
| TS1-P-002 | ResultBundle 引用不存在 artifact，或 artifact hash 不匹配 | 拒绝 | INV-005 |
| TS1-P-003 | 写入 allowlist 外路径 | 拒绝并保留执行证据 | INV-007 |
| TS1-P-004 | 对只读 ExperimentSpec 发起 mutation request | fail closed | G-002 / Q-006 |
| TS1-P-005 | IMPL 未 accepted 或候选 SHA 未冻结时让 VAL 进入 running | 拒绝，不改变 VAL 状态 | preconditions |
| TS1-P-006 | 引用未登记来源或缺失 `data_class` 的数据 | 拒绝 | INV-016 |
| TS1-P-007 | 使用陈旧 `expected_version` 提交 | 拒绝，要求重新读取 | Q-006 / version |

## 6. 组 3：Handoff / Result 幂等（`TS1-I-*`）

| ID | 输入/刺激 | 预期结果 | 关联 |
|---|---|---|---|
| TS1-I-001 | HandoffBundle SHA 不匹配 | 接收方拒收 | handoff integrity |
| TS1-I-002 | receiver 与 intended identity 不符 | 接收方拒收 | identity |
| TS1-I-003 | 同一 ResultBundle 重复提交 | 返回既有结果，不产生第二次 commit | INV-011 |
| TS1-I-004 | 对已失败 attempt 发起 retry | 创建新 attempt，旧记录与 workspace 保留 | INV-011 / Q-006 |
| TS1-I-005 | 提交被拒 | 拒绝记录与 ResultBundle 保留，不静默删除 | Q-006 |
| TS1-I-006 | 提交三层 hash 标注 | 正确区分 worker 自报、协调方 pre-Harness 复算、Harness 权威复算 | CT-001 |
| TS1-I-007 | VAL ResultBundle 缺少盲审节 | 拒绝审核提交 | 两段式审核 |
| TS1-I-008 | VAL 读取候选前候选 SHA 已变化 | fail closed，退回新 attempt | pre-Harness |

## 7. 组 4：回滚与恢复程序（`TS1-R-*`）

本组验证合同与 fixture 的回滚程序，不执行生产 canonical 写入。回滚不是免检通道。

| ID | 输入/刺激 | 预期结果 | 关联 |
|---|---|---|---|
| TS1-R-001 | 恢复上一份共享合同/TaskSlice revision | 创建新 revision，保留原记录并建立 supersedes，不原地覆盖 | revision |
| TS1-R-002 | 回滚后重算引用、SHA 与链接 | 引用完整、无迁移残桩、四组测试可在新基线复跑 | integrity |
| TS1-R-003 | 请求恢复 canonical 文件 | 没有 G-014 人工批准时拒绝；通过后核对 hash 与引用 | G-014 |
| TS1-R-004 | 请求恢复或改变 fixture 内容 | 没有 G-011 测试合同 Gate 时拒绝；通过后创建新 fixture version | G-011 |
| TS1-R-005 | 回滚过程试图删除/覆盖原始记录 | 拒绝，原版本保持可追溯 | immutable |

崩溃恢复、半提交、Research Event 原子事务和 Reconciler 实测属于 TS-002 的崩溃恢复/事务验收场景（编号待后续合同确定），本文件只冻结其合同语义，不在 TS-001 实装；不得与现有失败场景 F-001 混用。

## 8. 失败场景指针

| 失败场景 | 测试指针 | 处置 |
|---|---|---|
| F-038 | `TS1-S-005`、`TS1-S-006` | Schema fail closed |
| F-039 | `TS1-S-007`～`TS1-S-010` | 拒绝非法 identity、data_class、verdict 或重复 ID |
| F-040 | `TS1-S-011` | integrity rule/schema/Gate 缺失时 fail closed |
| F-041 | `TS1-P-001`～`TS1-P-003`、`TS1-P-006` | 引用或权限越界时拒绝 |
| F-042 | `TS1-P-004`、`TS1-P-005` | 只读对象 mutation 或未满足前置条件时拒绝 |
| F-043 | `TS1-P-007` | 陈旧版本提交时拒绝 |
| F-044 | `TS1-I-001`、`TS1-I-002` | Handoff 拒收 |
| F-045 | `TS1-I-003` | 重发不得产生第二次 commit |
| F-046 | `TS1-I-004` | retry 不得覆盖旧 attempt |
| F-047 | `TS1-I-005`、`TS1-I-006` | 拒绝记录保留，hash 三层标注不得混淆 |
| F-048 | `TS1-I-007` | 缺盲审节时拒绝审核提交 |
| F-049 | `TS1-I-008` | 候选漂移时 fail closed 并退回新 attempt |
| F-050 | `TS1-R-001`、`TS1-R-005` | 回滚不得覆盖历史 |
| F-051 | `TS1-R-003`、`TS1-R-004` | 回滚不得绕过 G-014/G-011 |
| F-052 | `TS1-R-002` | 回滚后完整性不通过则不得报告通过 |

## 9. 任务包门禁与验收状态

### 9.1 进入条件

- P0 整体获得用户批准；
- `TS001-IMPL` 与 `TS001-VAL` 的 TaskSlice 和共享合同 SHA 冻结；
- 纯数据 fixture manifest 与本清单 SHA 冻结；
- 测试运行目录不是生产 canonical 路径；
- 执行环境与推荐工具版本已经记录。

### 9.2 通过条件

只有同时满足以下条件，TS-001 才能报告 `PASS-ENGINEERING`：

- 四组用例全部执行；
- 所有正向用例通过，所有负向用例按预期拒绝；
- 七条直接覆盖的不变量逐条有证据；
- 回滚不覆盖历史，且 G-011/G-014 约束被验证；
- 无越界写入、无真实数据、无网络获取、无遗留残桩；
- Validation Agent 出具 `CONFORMANT`，并附命令与证据指针；
- 用户另行完成最终人类验收。

任何缺失运行、工具失败、输入漂移或证据不足都保持 `NOT-RUN` 或 `INCOMPLETE`，不得写成 PASS。

### 9.3 回滚验收记录

每次回滚必须记录：

1. 回滚前合同/fixture revision 与 SHA；
2. 触发原因与人工 Gate 指针；
3. 新 revision、supersedes 和变更字段；
4. 回滚后引用完整性、残桩扫描和四组测试结果；
5. 未验证项、阻塞项和下一 attempt。

## 10. 后置边界

- 不因本清单通过而批准 TaskSlice、ExperimentSpec 或 Claim；
- 不因工程 `PASS-ENGINEERING` 而产生科学支持或临床主张；
- 不在本切片选择生产实现语言、状态后端或网络沙箱技术；
- 不把测试 workspace、索引或日志当作 canonical 研究状态。
